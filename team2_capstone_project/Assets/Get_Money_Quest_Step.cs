using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Get_Money_Quest_Step : Dialogue_Quest_Step
{
    // Start is called before the first frame update
    void Start()
    {
        DelayedDialogue(0, 0);
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
